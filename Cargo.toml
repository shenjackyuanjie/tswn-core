[package]
name = "tswn-core"
description = "甚至是被技术封锁的名竞版本"
version = "0.1.8"
edition = "2024"

[dependencies]
# 用于 fast hashmap
foldhash = "0.2.0"
png = { version = "0.18", optional = true }

[dev-dependencies]
png = "0.18"

[features]
default = ["png_render"]
png_render = ["dep:png"]
