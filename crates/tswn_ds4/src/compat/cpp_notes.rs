//! 迁移 DS4 preview 时的 C++ 行为兼容性说明。
//!
//! - `duplicate.cpp` 保留原始行文本，并将 `\r` / `\n` 视为分隔符。
//! - `sort.cpp` 按所选分数降序、再按名称降序排序。
//! - `all_new.cpp` 按 `config.json` 中的 `team_name` 筛选新输入。
//! - 二人配对遍历左右候选全集；同集合配对只遍历上三角。
//! - 三人评分使用 ABCP5 分类结果，并和已有分类结果做增量组合。
