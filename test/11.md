# sampled_large_case_11_replay_should_match 失败分析（2026-02-28 更新）

## 复现摘要

- 复现命令：`cargo test sampled_large_case_11_replay_should_match -- --nocapture`
- 当前结果：失败，首分叉 `idx=47`
  - 期望：`fdgs发起攻击, adwada受到81点伤害`
  - 实际：`sdc发起攻击, fdgs受到100点伤害`
  - 附加现象：`actual_len=52`, `expected_len=53`

## cargo test 原文（摘录）

```text
sampled case-11 mismatch context [44..50):
  idx=44: actual=Some("adwada发起攻击, 使魔受到62点伤害, asdds受到31点伤害") | expected=Some("adwada发起攻击, 使魔受到62点伤害, asdds受到31点伤害")
  idx=45: actual=Some("asdds被击倒了") | expected=Some("asdds被击倒了")
  idx=46: actual=Some("使魔消失了") | expected=Some("使魔消失了")
  idx=47: actual=Some("sdc发起攻击, fdgs受到100点伤害") | expected=Some("fdgs发起攻击, adwada受到81点伤害")
  idx=48: actual=Some("fdgs被击倒了") | expected=Some("adwada被击倒了")
  idx=49: actual=Some("sdc发起攻击, adwada受到74点伤害") | expected=Some("幻影消失了")
```

## 关键现象

1. `idx=0..46` 完全对齐，分叉点出现在战斗后期（`使魔消失了`之后）。
2. 首分叉表现为不同的行动者：实际是`sdc`攻击`fdgs`，期望是`fdgs`攻击`adwada`。
3. 后续事件完全错位：实际中`sdc`继续行动，而期望中`sdc`直到后续才使用狂暴术。
4. 事件长度变化（52 vs 53），说明从分叉点开始整个战斗流程发生偏移。
5. 实际流程中，`sdc`连续行动两次击倒`fdgs`和`adwada`，而期望流程中`fdgs`先击倒`adwada`，然后`幻影消失`，最后`sdc`使用狂暴术击倒`fdgs`。

## 问题分类

**行动顺序/目标选择** — 分叉点出现在"使魔消失"后，此时应为`fdgs`的回合，但实际是`sdc`行动。可能原因包括：
1. 角色行动顺序计算与预期不一致
2. 目标选择逻辑在特定状态下产生分歧
3. 状态机在召唤物消失后的回合计算存在差异

## 潜在根因分析

根据之前的测试失败模式，此问题可能与以下因素有关：
1. **随机种子差异**：虽然玩家名称相同，但可能缺少团队后缀（如`@team425`），导致`raw_namerena_to_idname()`生成不同的随机种子。
2. **状态依赖性**：战斗后期状态复杂（召唤物消失、多个角色状态变化），可能导致行动顺序计算出现分歧。
3. **技能触发条件**：期望中`fdgs`应使用"净化"后继续行动，但实际中`sdc`获得了额外行动机会。

## 备注

- 测试用例11是新添加的测试，与其他测试用例的玩家命名格式可能不一致（缺少`@team425`后缀）。
- 需要验证玩家名称格式是否与其他通过的测试用例（如case_08）一致。
- 此失败模式与case_07相似，都是行动顺序和目标选择的问题。