# 测试失败清单（2026-03-01）

本文件仅保留当前运行 `cargo test` 后仍未通过的测试用例清单，便于聚焦修复工作。
最近一次运行命令：`cargo test -- --nocapture`（在本仓库根目录下执行），结果为 84 通过，6 失败。

失败用例（按测试套件/用例名列出）：

| 测试标识 | 首个分叉 idx | 期望总条目数（预估） | 实际总条目数（预估） | 简要现象 | 参考文档 |
| --- | ---: | ---: | ---: | --- | --- |
| `engine::test::runner::large_31_40::large_32` | 36 | 97 | 87 | 技能选择错误（加速术 vs 攻击），后续行动错位 | `test/32.md` |
| `engine::test::runner::large_31_40::large_34` | 16 | 82 | 88 | 技能选择错误（攻击 vs 生命之轮），后续行动错位 | `test/34.md` |
| `engine::test::runner::large_31_40::large_35` | 36 | 80 | 80 | 状态效果应用错误（诅咒状态未显示） | `test/35.md` |
| `engine::test::runner::large_31_40::large_36` | 30 | 80 | 91 | 技能伤害计算错误（66 vs 132伤害） | `test/36.md` |
| `engine::test::runner::large_31_40::large_37` | 14 | 73 | 73 | 防御伤害计算错误（1 vs 0伤害） | `test/37.md` |
| `engine::test::runner::fight_large::large_full` | 135 | 677 | 703 | 瘟疫技能命中判定差异（回避 vs 百分比伤害） | `test/large.md` |

说明与建议
1. 本表仅包含仍然失败的测试项；之前已修复或通过的测试项已从此表中移除，便于集中精力处理当前问题。
2. 从失败类型来看，优先级建议：
   - 首先检查"技能触发/选择"与"目标选择"相关逻辑（这些通常会引发链式偏移）。
   - 其次复核"伤害/命中"计算路径及随机数序列一致性。
   - 对于"状态效果"类问题（如诅咒状态未显示），检查状态应用时机和显示逻辑。
3. 调试建议：
   - 在出现分叉的 idx 点前后加入更细粒度的日志（包括随机种子/序列位置、行动队列、可用技能/冷却、目标评分/优先级）。
   - 对照期望输出与实际输出，定位第一个不匹配的"状态差异"（例如某角色的 HP、状态、或随机数流已不同）。
   - 对可复现的单个失败用例，尝试用确定性复现（锁定 seed）逐步缩小差异源头。
4. 文档位置：
   - 每个失败用例对应的详细分析与 cargo 输出片段保存在 `test/*.md`（例如 `test/32.md` 等），请从这些文件开始排查。
