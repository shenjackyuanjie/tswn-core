# sampled_large_case_18_replay_should_match 失败分析（2026-03-01 更新）

## 复现摘要
- 复现命令（在仓库根目录下运行）：
  - `cargo test engine::test::runner::large_18_22::large_18 -- --nocapture`
- 当前结果：失败，首个分叉（first mismatch）发生在 `idx=35`
  - 期望（expected）：`4iJ53RqFn8发起攻击, OD2wlIdqr1受到64点伤害`
  - 实际（actual）：`4iJ53RqFn8使用分身, 出现一个新的4iJ53RqFn8`
  - 附加信息：`actual_len=58`, `expected_len=57`（事件总数有轻微差异）

## cargo test 原文（关键摘录）
（从测试输出抓取的上下文片段）
```
sampled case-18 mismatch context [32..38):
  idx=32: actual=Some("Omj8qVJppc发起攻击, 4iJ53RqFn8回避了攻击") | expected=Some("Omj8qVJppc发起攻击, 4iJ53RqFn8回避了攻击")
  idx=33: actual=Some("xwjcqObl2L发起攻击, 4iJ53RqFn8受到1点伤害") | expected=Some("xwjcqObl2L发起攻击, 4iJ53RqFn8受到1点伤害")
  idx=34: actual=Some("xbws21Im4P发起攻击, 4iJ53RqFn8受到1点伤害") | expected=Some("xbws21Im4P发起攻击, 4iJ53RqFn8受到1点伤害")
  idx=35: actual=Some("4iJ53RqFn8使用分身, 出现一个新的4iJ53RqFn8") | expected=Some("4iJ53RqFn8发起攻击, OD2wlIdqr1受到64点伤害")
  idx=36: actual=Some("OD2wlIdqr1发起攻击, xwjcqObl2L回避了攻击") | expected=Some("OD2wlIdqr1被击倒了")
  idx=37: actual=Some("4iJ53RqFn8使用分身, 出现一个新的4iJ53RqFn8") | expected=Some("4iJ53RqFn8使用减速术, xwjcqObl2L进入迟缓状态")
```

## 关键现象
1. 在 `idx=32..34` 之前，实际与期望完全对齐，分叉出现在中段（idx=35）。
2. 分叉类型：动作类型发生变化 —— 期望为“普通攻击”，实际触发了“分身”技能（clone/summon），导致战场单位数发生变化，从而引起后续事件错位（链式偏移）。
3. 分叉后不仅第 35 条事件不同，随后的多条事件也被替换或错位（例如期望 OD2wlIdqr1 被击倒，但实际没有），说明这不是单一数值误差，而是改变了战局状态（单位列表 / 行动队列）。
4. 事件总数出现微小差异（58 vs 57），与分身导致的单位数量变化一致。

## 初步问题分类
- 技能触发逻辑问题（Skill trigger）：角色 `4iJ53RqFn8` 在该时刻是否满足“分身”技能触发条件与期望不一致。
- 优先级/决策逻辑（Decision/priority）：在存在可用技能与普通攻击时，系统选择了不同的行为（分身 vs 攻击）。
- 随机流/判定差异（RNG / conditional）：若技能触发包含随机判定或概率判定，随机数序列在该点可能已偏移或条件判断路径不同。

## 可能的根因（按优先级排序）
1. 分身技能的触发条件实现与期望不一致（例如：条件判定漏检、状态误判断或冷却/次数逻辑错误）。
2. 决策逻辑中技能优先级被错误设置或在特定状态下优先级计算出错，导致在应攻击时触发了分身。
3. 随机数序列漂移或在前几条事件中使用 RNG 的次数不一致，进而改变了到达 idx=35 时的随机判定结果。
4. 行动队列或单位生命周期（如临时召唤/死亡清理）在边界处处理不一致，导致“分身”与“击倒”事件互相替代。

## 建议的排查步骤（可依次执行）
1. 确认该测试的 deterministic seed：如果测试支持锁定 seed，先用固定 seed 多次重放，观察是否每次稳定重现分叉。
   - 若可重现，说明非随机偶发；若不可重现，需抓取 RNG 序列/次数。
2. 在触发点前后打印/记录状态快照（在测试运行时临时添加调试打印）：
   - 记录 `4iJ53RqFn8` 的状态：HP/MP、冷却、已知标志（是否有分身可用）、技能可用性标志。
   - 记录行动队列当前顺序与队列快照（谁将要行动、优先级值）。
   - 记录 RNG 的当前索引或下一个随机值（便于比对）。
3. 检查 `分身` 技能实现：
   - 触发条件（条件是否包括 HP、状态、概率、冷却、拥有者等）是否与文档/期望一致。
   - 分身产生的单位是否正确加入战场并进入行动队列（添加时机是否会影响当轮判定）。
4. 对比“期望行为”的实现路径（测试期望是“攻击 OD2wlIdqr1 且造成 64 点伤害”），找到触发那条期望路径的代码分支，并确认在实际运行中对应分支未被选中，定位阻断原因。
5. 在测试框架层面，增加更细的差异捕捉日志：在比较 actual vs expected 前，把两侧的“关键实体快照”并列输出，便于快速定位哪一项属性出现差异（如某角色的 cooldown、状态标志不同）。
6. 若怀疑 RNG 漂移：从测试开始到分叉点，记录每次调用 RNG 的位置（计数器），确保实际与期望使用 RNG 的次数一致。必要时使用 deterministic fake RNG（逐步替换）来定位差异来源。

## 具体可执行的临时调试改动（建议）
- 在 `engine` / `runner` 的测试辅助代码中，针对 `large_18` 用例插入一段：在事件序列第 30..36 条前后，dump 以下信息到测试输出（--nocapture）：
  - 当前 seed / RNG 索引
  - 行动队列快照（actor id, priority, hp, 状态列表）
  - `4iJ53RqFn8` 的技能可用性（每个技能的冷却/剩余次数/触发条件评估结果）
  - 若调用技能选择逻辑函数，打印该函数的输入（候选技能列表、评分、选中理由）
- 添加单元测试覆盖分身技能的触发条件（边界情况：冷却为 0、HP 临界、目标存在/不存在等），确保实现与期望一致。

## 结论与下一步
- 本用例的分叉由“分身技能被触发替代攻击”引起，属于技能触发/决策优先级方向的问题，而非单纯数值误差。
- 建议按上面“排查步骤”逐步定位：先锁定可复现性并收集分叉点前的状态快照；然后检查分身技能及决策逻辑；最后确认 RNG 与队列处理是否一致。
- 如果你希望，我可以在你允许修改测试/代码的情况下，帮助生成具体的临时调试补丁（插入日志）以及示例输出格式，以便在本地快速定位问题。