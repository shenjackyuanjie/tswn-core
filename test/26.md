# sampled_large_case_26_replay_should_match 失败分析（2026-03-01 更新）

## 复现摘要
- 复现命令（在仓库根目录下运行）：
  - `cargo test engine::test::runner::large_23_30::large_26 -- --nocapture`
- 当前结果：失败，首个分叉（first mismatch）发生在 `idx=16`
  - 期望（expected）：`tTXJKvz97a发起攻击, uiR8YhB0SU受到52点伤害`
  - 实际（actual）：`tTXJKvz97a发起攻击, cwUzcrsQuj受到39点伤害`
  - 附加信息：`actual_len=86`, `expected_len=93`（事件总数有差异）

## cargo test 原文（关键摘录）
```
sampled case-26 mismatch context [13..19):
  idx=13: actual=Some("ShWe7Y3aZO发起攻击, uiR8YhB0SU受到89点伤害") | expected=Some("ShWe7Y3aZO发起攻击, uiR8YhB0SU受到89点伤害")
  idx=14: actual=Some("DcW01V95mE发起攻击, DWk4uqD7r8受到151点伤害") | expected=Some("DcW01V95mE发起攻击, DWk4uqD7r8受到151点伤害")
  idx=15: actual=Some("cwUzcrsQuj使用分身, 出现一个新的cwUzcrsQuj") | expected=Some("cwUzcrsQuj使用分身, 出现一个新的cwUzcrsQuj")
  idx=16: actual=Some("tTXJKvz97a发起攻击, cwUzcrsQuj受到39点伤害") | expected=Some("tTXJKvz97a发起攻击, uiR8YhB0SU受到52点伤害")
  idx=17: actual=Some("GKNTjhqO96使用地裂术") | expected=Some("GKNTjhqO96发起攻击, uiR8YhB0SU受到45点伤害")
  idx=18: actual=Some("cwUzcrsQuj受到31点伤害") | expected=Some("uiR8YhB0SU发起攻击, NDCGJvqzmQ受到44点伤害")
```

## 关键现象
1. 在 `idx=15` 之前，实际与期望完全对齐，分叉出现在中段（idx=16）。
2. 分叉类型：目标选择差异 —— 同一攻击者（tTXJKvz97a），期望攻击 uiR8YhB0SU，实际攻击 cwUzcrsQuj（分身召唤出的新单位）。
3. 事件总数出现差异（86 vs 93），说明分叉后战局状态发生显著变化，后续事件序列严重错位。
4. 分身技能的召唤导致战场单位数发生变化，从而改变了行动队列和目标选择逻辑。

## 问题分类
**目标选择** — 攻击动作正确，但目标选择与预期不一致。分身召唤出的新单位 cwUzcrsQuj 被错误地选为目标，而非原来的 uiR8YhB0SU。这涉及目标选择逻辑、召唤单位处理、行动队列排序。

## 可能根因
1. 分身召唤出的单位被错误地加入目标候选列表，导致目标选择逻辑选择了新召唤的单位。
2. 行动队列排序逻辑在有召唤单位时发生变化，导致攻击顺序/目标优先级不同。
3. 随机数序列偏移导致目标选择算法产生了不同的结果。
4. 目标选择评分函数在召唤单位存在时的计算逻辑不一致。

## 调试建议
1. 检查 idx=16 之前的战斗状态：cwUzcrsQuj 分身是否被正确创建，行动队列顺序如何。
2. 检查 tTXJKvz97a 的目标选择逻辑：候选目标列表包含哪些单位？评分标准是什么？
3. 确认分身单位的属性（阵营、是否敌对）是否影响目标选择。
4. 检查行动队列中召唤单位的处理顺序。
