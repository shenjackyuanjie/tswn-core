# sampled_large_case_30_replay_should_match 失败分析（2026-03-01 更新）

## 复现摘要
- 复现命令（在仓库根目录下运行）：
  - `cargo test engine::test::runner::large_23_30::large_30 -- --nocapture`
- 当前结果：失败，首个分叉（first mismatch）发生在 `idx=63`
  - 期望（expected）：`8TENSACVdO的铁壁被打消了`
  - 实际（actual）：`UfjGOy7sst发起攻击, 使魔受到128点伤害, U0TvbhRUZT受到64点伤害`
  - 附加信息：`actual_len=80`, `expected_len=82`（事件总数有差异）

## cargo test 原文（关键摘录）
```
sampled case-30 mismatch context [60..66):
  idx=60: actual=Some("UfjGOy7sst发起攻击, tWsPQmTVhW受到74点伤害") | expected=Some("UfjGOy7sst发起攻击, tWsPQmTVhW受到74点伤害")
  idx=61: actual=Some("tWsPQmTVhW被击倒了") | expected=Some("tWsPQmTVhW被击倒了")
  idx=62: actual=Some("VIDvuBJCM4使用净化, 8TENSACVdO受到1点伤害") | expected=Some("VIDvuBJCM4使用净化, 8TENSACVdO受到1点伤害")
  idx=63: actual=Some("UfjGOy7sst发起攻击, 使魔受到128点伤害, U0TvbhRUZT受到64点伤害") | expected=Some("8TENSACVdO的铁壁被打消了")
  idx=64: actual=Some("使魔消失了") | expected=Some("UfjGOy7sst发起攻击, 使魔受到128点伤害, U0TvbhRUZT受到64点伤害")
  idx=65: actual=Some("UfjGOy7sst从铁壁中解除") | expected=Some("使魔消失了")
```

## 关键现象
1. 在 `idx=62` 之前，实际与期望完全对齐，分叉出现在中后段（idx=63）。
2. 分叉类型：行动顺序差异 —— 期望中 8TENSACVdO 的铁壁状态应先被打消，然后 UfjGOy7sst 才发起攻击；实际中 UfjGOy7sst 先发起攻击，然后铁壁才解除。
3. 事件总数出现差异（80 vs 82），说明分叉后战局状态发生变化。
4. 使魔的攻击和消失顺序也发生了变化（实际中使魔先攻击后消失，期望中先消失后攻击）。

## 问题分类
**行动顺序/状态处理** — 铁壁状态的解除与攻击行动的顺序不一致。这涉及状态效果处理顺序、行动队列排序、技能效果触发时机。

## 可能根因
1. 铁壁状态被打消的触发条件与实际执行时机不一致。
2. 行动队列排序逻辑导致 UfjGOy7sst 的行动早于 8TENSACVdO 的铁壁解除。
3. 使魔的消失逻辑（死亡/技能结束）与攻击行动的执行顺序不一致。
4. 随机数序列偏移导致状态效果判定时机不同。

## 调试建议
1. 检查 idx=63 之前 8TENSACVdO 的状态：铁壁剩余持续时间、触发条件。
2. 确认铁壁状态被打消的时机：是被攻击后打消？还是时间到期自动解除？
3. 检查 UfjGOy7sst 和 8TENSACVdO 的速度属性和行动队列顺序。
4. 确认使魔的消失逻辑与攻击行动的执行顺序关系。
