# tswn_core 0.2.20 更新说明（继续回收 win-rate 性能回退）

> `0.2.20` 这一版的目标很单一：在 `0.2.19` 已经把 `3v3v3 + maxcase=4000` 的大样本 diff 收口到 `failed case = 0` 之后，继续把 `bench win-rate` 路径里最明显的性能回退往回收。
>
> 这轮真正保留下来的改动，集中在三条已经验证过 correctness 的热路径：
>
> - dynamic `pre_action`
> - `Hide` 的 same-action alive 视图
> - 一批主动技能里的 alive-group 长度查询
>
> 此外，还补齐了一组输入路径的 **UTF-8 BOM 兼容性修复**、目标选择热路径的 **single-candidate 提前返回** 微优化，以及构建文档的 **uv 迁移**。
>
> 对应简版条目见：`crates\tswn_core\CHANGELOG.md`

---

## 1. 改动目标

`0.2.19` 已经把这一轮 correctness / parity 风险最大的部分收住了：

- `cargo test -p tswn_core` 能稳定通过
- `tests\sqp5900.txt`、`3v3v3`、`max-cases-per-mode=4000` 口径下，`failed case = 0`

但当时按 `docs\performance.md` 的固定口径补跑 benchmark 后，`0.2.19` 相对 `0.2.14` 仍然有稳定回退，尤其多线程更明显。

因此，`0.2.20` 的出发点不是再扩大语义修复范围，而是：

1. 只保留能确认不打坏 parity 的性能改动；
2. 优先改那些覆盖范围大、行为容易证明等价的热路径；
3. 对 mixed-signal 的优化直接撤回，不把 speculative 复杂度带进版本里；
4. **顺带补齐了输入路径的兼容性修复与构建文档的 uv 迁移**。

---

## 2. 主要改动

## 2.1 `SkillStorage` 的 dynamic `pre_action` 热路径继续收紧

涉及文件：

- `crates\tswn_core\src\player\skill\store.rs`
- `crates\tswn_core\src\player\impl_runtime.rs`

这轮把 `pre_action` 相关路径继续压了一遍：

- 去掉 `pre_action()` 主循环里的 `self.pre_action.clone()`
- 减少循环中的重复 `contains()` / lookup
- 把 dynamic `pre_action` 的 membership 独立成哈希集，保留 `Vec` 作为顺序视图
- `impl_runtime` 在 skill act 后直接复用当前 skill 上已知的 dynamic pre_action 状态，不再重新查一遍 store

核心目标是减少：

- 每轮 pre_action 遍历时的额外 clone
- 动态启停 pre_action 时的线性存在性检查
- action 热路径上的重复查表

这部分属于"行为不变、只减少重复工作"的优化。

## 2.2 `Hide` 改成借引用 + iterator + `SmallVec`

涉及文件：

- `crates\tswn_core\src\engine\storage.rs`
- `crates\tswn_core\src\player\skill\skl\hide.rs`

`Hide` 这一轮继续保留了已经验证过的一组低风险优化：

- 不再 clone alive group / roster group
- `Storage` 新增 iterator 风格的：
  - `iter_pending_spawn_ids_for_owner(...)`
  - `iter_pending_revival_ids_for_group(...)`
- `Hide` 改用 borrowed group 视图和 `SmallVec<[PlrId; 8]>` 聚合 alive candidates

这样既保留了 `0.2.19` 已经对齐好的 same-action 可见性语义，也去掉了几段在 post_damage 热路径里反复创建中间 `Vec` 的成本。

## 2.3 alive-group 长度查询改成基于 team index 的 O(1) 路径

涉及文件：

- `crates\tswn_core\src\engine\storage.rs`
- `crates\tswn_core\src\player\skill\act\assassinate.rs`
- `crates\tswn_core\src\player\skill\act\berserk.rs`
- `crates\tswn_core\src\player\skill\act\charm.rs`
- `crates\tswn_core\src\player\skill\act\curse.rs`
- `crates\tswn_core\src\player\skill\act\disperse.rs`
- `crates\tswn_core\src\player\skill\act\exchange.rs`
- `crates\tswn_core\src\player\skill\act\half.rs`
- `crates\tswn_core\src\player\skill\act\slow.rs`

之前这一批主动技能在 smart target score 里都会反复做：

```rust
alive_group_containing(target).map(|group| group.len()).unwrap_or(0)
```

它的真实成本是：

- 先在 `alive_groups` 上做一次线性扫描
- 找到组后再取长度

现在 `Storage` 新增了：

- `alive_group_len_containing(...)`

这条 helper 直接基于已有 `player_group` / team index 反查 alive_group，再确认 actor 确实还在该组里，语义和旧实现保持一致，但把这条高频查询从"扫整份 alive_groups"降成了"定点取组 + 常数级确认"。

由于它覆盖了 8 个主动技能的目标打分，所以这轮收益比单独抠某一个 skill 更稳定。

## 2.4 目标选择热路径补充 single-candidate 提前返回

涉及文件：

- `crates\tswn_core\src\player\skill.rs`
- `crates\tswn_core\src\player\impl_runtime.rs`

在目标选择的核心路径中，当 `selected` 只包含一个候选目标时，完整的 scoring + ranking 流水线是不必要的——只有一个目标，名次必然是第一名。

本轮在以下四个入口增加了 `selected.len() == 1` 提前返回：

- `SkillTrait::pick_smart_target()`（trait 默认实现）
- `SkillTrait::pick_smart_target_with_level()`（trait 默认实现）
- `Player::pick_targets()`（`impl_runtime`）
- `Player::pick_forced_attack_target()`（`impl_runtime`）

提前返回时：

- 直接对唯一目标执行 `score_target()`（确保 side-effect 一致）
- 不再组装 `SmallVec`、不再排序、不再做二次过滤
- 在 `pick_forced_attack_target()` 中保留了完整的 debug 日志逻辑（`#[cfg(not(feature = "no_debug"))]`）

这条改动不影响任何语义——无论一个目标还是多个目标，最终的选人结果一致——但省去了 `map` / `sort_by` / `collect` 的固定开销。在单目标候选频繁出现的场景下（例如只剩一个人存活时），收益是可叠加的。

## 2.5 `tick.rs` 的 `ally_dead` 改用 `is_alive_now` 直接判断

涉及文件：

- `crates\tswn_core\src\engine\tick.rs`

改动点：

```rust
// 之前：从 team_roster 中过滤出不在 ally_alive 中的死尸
let ally_dead: PlrVec = team_roster.iter().copied()
    .filter(|id| !ally_alive.contains(id)).collect();

// 之后：直接复用 is_alive_now 谓词
let ally_dead: PlrVec = team_roster.iter().copied()
    .filter(|id| !is_alive_now(id)).collect();
```

`ally_alive` 上面一行已经通过 `is_alive_now` 从 `team_roster` 中构建完毕，所以原本的 `!ally_alive.contains(id)` 等价于 `!is_alive_now(id)`，但 `contains` 对 `PlrVec`（`Vec<PlrId>`）做线性扫描，而 `is_alive_now` 通过 `player_group` 直接判定，是 O(1) 操作。

这条改动属于"去掉冗余的线性 contains，用等价的 O(1) 谓词替代"。

## 2.6 UTF-8 BOM 兼容性修复

涉及文件：

- `crates\tswn_core\src\bin\tswn_case_miner.rs`
- `crates\tswn_core\src\bin\tswn_cli\args.rs`
- `crates\tswn_ds3\src\config.rs`

当输入文件带有 UTF-8 BOM（`U+FEFF`）时，`fs::read_to_string` 会原样保留该前缀，导致首行解析异常（例如以 `\u{feff}` 开头的字符串无法匹配预期的模式）。

本轮修补了三条输入路径：

1. **`tswn_case_miner.rs`**：
   - `load_library()` 在读取号库后 strip BOM
   - `read_first_line()` 在读取首行后 strip BOM
   - 新增 `strip_utf8_bom()` 工具函数

2. **`tswn_cli/args.rs`**：
   - `read_stdin()` 在处理管道输入后 strip BOM
   - `read_file()` 在读取文件后 strip BOM
   - 新增同名的 `strip_utf8_bom()` 工具函数（独立于 case_miner，两者是不同的二进制目标）

3. **`tswn_ds3/src/config.rs`**：
   - `Config::load_from_path()` 在解析 JSON / YAML 前 strip BOM

三者分别覆盖了：号库输入、CLI 原始 namerena 输入、以及 DS3 配置文件的入口。

修复方式一致——`strip_prefix('\u{feff}').unwrap_or(s)`——零成本地跳过 BOM 前缀。

## 2.7 构建文档与脚本说明迁移至 uv

涉及文件：

- `docs\build_all.md`
- `scripts\README.md`

### `docs/build_all.md`

全面更新为 `uv run` 驱动的构建流程：

- 前提章节改为 `uv` + `cargo`，移除旧的裸 `python` 依赖
- 各构建步骤的命令从 `python scripts/...` 改为 `uv run scripts/...`
- 新增 **WSL/Linux 纯环境构建** 章节（仅构建 Linux 产物，不依赖 Windows 侧）
- 新增 **uv 用户的 WSL 增强方案** 章节（WSL 侧也安装 uv 后统一管理）
- 新增 **备选：单个步骤的 uv 快捷命令** 章节
- 末尾新增 **版本对照表**（tswn_core / tswn_capi / tswn_py 版本标齐）

### `scripts/README.md`

- 所有命令示例统一从 `python scripts/...` 更新为 `uv run scripts/...`
- 补充 `uv` 环境说明：`.venv`（Windows 侧）与 `.venv-wsl`（WSL 侧）的定位和关系

---

## 3. 撤回的尝试

这轮并不是所有尝试都保留了。

其中一组比较典型的是：

- `Protect` / `Counter` 的 group clone 消除

这条线在局部 spot-check 时看起来有正信号，但在后续 A/B 复测里表现成 mixed signal，没有形成"两个样本都稳定更快"的净收益，因此最终撤回，没有带进 `0.2.20`。

这也是本轮一个刻意坚持的原则：

- **没有证据证明稳定收益的优化，不留。**

---

## 4. 结果与 benchmark

当前建议继续参考 `docs\performance.md` 的固定口径：

- `--release --features no_debug`
- `tswn-cli bench win-rate ... --perf`
- `100k` 与 `1M`
- 单线程 / CLI 默认多线程

这轮同步回文档的结果是：

| 样本 | 单线程 `100k` | 多线程 `100k` | 单线程 `1M` | 多线程 `1M` |
|---|---:|---:|---:|---:|
| `aaa` vs `bbb` | `1.887s` | `0.291s` | `18.438s` | `2.784s` |
| `复杂样本` | `2.891s` | `0.471s` | `28.488s` | `3.749s` |

和 `0.2.14` 基线相比：

- 单线程当前慢约 `1.6%~2.3%`
- 多线程当前慢约 `7.9%~10.7%`

和上一轮 `0.2.19` 文档口径相比，这一版已经把回退继续收窄了一截，尤其 `1M` 口径更明显：

- `aaa` `1M` 单线程：`19.006s -> 18.438s`
- `aaa` `1M` 多线程：`2.836s -> 2.784s`
- `复杂样本` `1M` 单线程：`29.064s -> 28.488s`
- `复杂样本` `1M` 多线程：`3.886s -> 3.749s`

也就是说，`0.2.20` 当前可以明确确认的是：

- **correctness 继续稳住了**
- **大样本 parity 继续维持 `failed case = 0`**
- **性能回退继续缩小了，但多线程仍没完全回到 `0.2.14`**

---

## 5. 当前 git 修改摘要

当前真正保留下来的改动，可以概括成 6 组：

1. **dynamic `pre_action` 热路径**
   - clone / contains / redundant lookup 精简
   - membership set 化

2. **`Hide` / same-action alive 视图**
   - iterator helper
   - borrowed group
   - `SmallVec` 聚合

3. **alive-group 计数路径**
   - 新增 `alive_group_len_containing(...)`
   - 8 个高频主动技能统一切换到 O(1) 路径

4. **目标选择 single-candidate 提前返回**
   - `skill.rs` trait 两个默认方法
   - `impl_runtime.rs` 两个入口
   - 跳过完整 scoring + ranking 流水线

5. **输入路径 UTF-8 BOM 兼容**
   - `tswn_case_miner` / `tswn-cli` / `tswn_ds3` 三处统一处理
   - 覆盖号库、CLI 输入、配置文件的 BOM 前缀

6. **文档与构建流程更新**
   - `docs/build_all.md`：uv 驱动、WSL/Linux 纯环境构建、版本对照表
   - `scripts/README.md`：命令示例与环境说明统一更新
   - `docs\performance.md`：benchmark 口径同步
   - 本文档
   - `CHANGELOG.md`

---

## 6. 版本信息

本轮更新后：

- `tswn_core` 版本从 `0.2.19` 步进到 `0.2.20`

这一版更适合作为"继续回收性能回退"的版本发布：

- correctness / parity guardrail 已经在
- 保留的优化都经过当前回归口径复核
- 还没有被证明稳定收益的尝试没有被带进版本

---

## 7. 建议验证

本轮建议至少验证：

- `cargo test -p tswn_core`
- `python .\track_case_miner.py --library .\tests\sqp5900.txt --modes 3v3v3 --case-offset-per-mode 0 --max-cases-per-mode 4000 --keep-going`
- `cargo run --release --features no_debug --bin tswn-cli -- bench win-rate "aaa" "bbb" -n 1000000 --single-thread --perf`
- `cargo run --release --features no_debug --bin tswn-cli -- bench win-rate "aaa" "bbb" -n 1000000 --perf`
